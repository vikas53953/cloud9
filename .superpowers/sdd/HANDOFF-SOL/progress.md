# SDD ledger — plan: C:\Users\vikasmit\cloud9\HANDOFF-SOL.md

## Feature 1 — shared artifact workspace
- Status: IN PROGRESS
- Baseline re-run by Sol on 2026-08-02 at `e738083`: build clean; 1,002 passed, 0 failed; QA 494/494 + 8/8 + 4/4; all commands exit 0. Full logs: `docs/qa/sol-baseline-*.log`.
- Product decision approved by Vikas on 2026-08-02: files are room-visible by default; room managers may restrict one file to selected current human members of that room; room managers remain included; access can never broaden beyond the room.
- I chose **Files** as the screen name — Vikas may overrule. This applies the standing plain-words law and the existing “Files agents made” wording.
- I chose same-room typed links only in this first release — Vikas may overrule. Cross-room links would expose names across different permission walls.
- I chose agent-declared links through a typed private manifest for the turn, not inferred from markdown and not manually edited on screen — Vikas may overrule. The screen reads and follows proven links; it does not guess provenance.
- Existing 20-version retention stays. “Immutable” means an old retained version row/bytes are never edited or reused; pruning the oldest version remains the documented retention rule.
- Review page for the permission decision: `docs/gates/sol-feature1-permissions.html`.
- Shared contract fix round 1/5: 7 addressed, 0 open. Scoped re-review APPROVED — private stored links and public projected versions are separate; hidden relation shapes cannot carry target details; strict mutation/list validators and real wire-contract boundary tests are pinned.
- Shared slice: complete for feature integration. Universal optional request IDs, relation bound, privacy split, validators and contract documentation all received APPROVED re-reviews; worker-only shared run now 90 passed, 0 failed — provisional until Sol's final evidence chain.
- Engine fix round 1/5: normalised filename ambiguity and stale-byte binding addressed; manifest cap/refusal privacy also tightened. Re-review left 1 open — all-turn-path coverage still tested source shape rather than the real chat/task/schedule/worktree doors.
- Engine fix round 2/5: runtime entry-path proof added, but byte-state binding still fails on Windows when content changes before the first SHA read; changed candidates also consume the 10-file cap without backfill. This is the second failed attempt on the same class.
- Engine root cause confirmed: a produced offer was a mutable path recipe, not owned bytes; Windows metadata can all remain unchanged across a same-size rewrite, and the first later SHA can bless the wrong content.
- I chose engine-owned one-read Buffer snapshots, no publish-time filesystem access, and `maxFiles` after successful captures — Vikas may overrule. This is the architecture verdict from the root-cause pass.
- Engine fix round 3/5: immutable Buffer snapshots and successful-capture backfill APPROVED; original SHA race and slot-loss findings addressed. Re-review found 1 new Important gap: capture backfill could read all 5,000 candidates without a practical work ceiling.
- Engine fix round 4/5: separate 200 MB capture-work ceiling addressed and re-review APPROVED. Engine slice complete for feature integration: pathless Buffer snapshots, 100 MB retained cap, successful-capture backfill, zero publish-time filesystem access, and real entry-path attribution all review clean. Worker/reviewer engine run: 603 passed, 0 failed — provisional until Sol's final evidence chain.
- Desktop fix round 1/5: privacy cache, bounded list, request lifecycle, role refresh, unsaved navigation and honest unavailable/truncation states addressed. Re-review left 1 Important open: a clean local access draft can be mistaken for dirty after an external access update.
- Desktop fix round 2/5: access baseline and artifact/workspace request identity addressed; all original findings closed. Re-review found 1 new Important blocker: permission-save success could be settled by an unrelated same-file push.
- Desktop fix round 3/5: exact permission-save success identity addressed. Re-review found 1 new Important blocker: refusal frames had no request ID and the generic error handler blindly shifted the first ledger row, so fire-and-forget sends could steal a save refusal.
- Desktop fix round 4/5: exact-ID refusals and fire-and-forget ledger omission addressed. Re-review found 1 new Important generic edge: a no-ID refusal from a later fire-and-forget operation could still settle an unrelated older legacy request.
- Desktop fix round 5/5: universal send-boundary request IDs and exact refusal routing addressed; breaker re-review APPROVED. Desktop slice complete for feature integration — all original and follow-on Files/privacy/pagination/permissions/request-ledger findings review clean. Worker/reviewer deterministic request exercise 15/15 plus typecheck/build green — provisional until Sol's final evidence chain.
- Relay security review: no high-confidence High/Medium exploit found.
- Relay fix round 1/5: production storage, invalidation, malformed-row, atomic membership, relation bound, pagination, access-summary, staging cleanup and request-ID findings addressed. Re-review left 1 Important evidence gap: missing-exact-target test asserted no row but not no staged bytes.
- Relay fix round 2/5: missing-target no-row/no-byte fail-back proof addressed. Re-review confirms all original findings addressed but found 3 new Important blockers: final rename directory not flushed before DB commit; PID-only stage ownership mishandles reuse/legacy names; unchanged public relation pushes leak hidden activity timing.
- Relay fix round 3/5: directory durability, projection-diff timing privacy and universal refusal IDs addressed. Re-review found 1 Important gap: v2 stage parser discarded the startup nonce, so a recent stale stage with a reused current PID could be preserved.
- Relay fix round 4/5: cleanup nonce handling and pre-auth request-ID echo improved. Breaker review found 1 Critical and 1 Important implementation blocker plus two test gaps: valid non-object JSON could crash before safe refusal; append could promote/delete a foreign v2 stage; old current-pid/current-nonce age and reused-invite ID lacked proof.
- Relay fix round 5/5 IN PROGRESS — fresh Opus worker adding one validated pre-auth frame boundary and strict stage-append ownership. This is the final relay round; residual load-bearing failure will block the feature.
